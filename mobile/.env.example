# Optional full override. Physical devices cannot use localhost for an API
# running on your computer, so use its LAN address when setting this value.
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5001/rest/v1

# Used by automatic local API discovery when EXPO_PUBLIC_API_URL is unset.
EXPO_PUBLIC_API_PORT=5001
